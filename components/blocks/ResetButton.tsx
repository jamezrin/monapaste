import styled from '@emotion/styled';

const ResetButton = styled.button`
  background: none;
  border: inherit;
  cursor: pointer;
  outline: inherit;
  font: inherit;
  appearance: none;
`;
export default ResetButton;
