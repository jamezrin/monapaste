import styled from '@emotion/styled';

const ResetSelect = styled.select`
  background: none;
  border: inherit;
  cursor: pointer;
  outline: inherit;
  font: inherit;
  appearance: none;
`;

export default ResetSelect;
